// Top-level build file
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.2.0'
    }
}

// 注意：项目依赖的仓库配置在 settings.gradle 的 dependencyResolutionManagement 中统一定义
